name: Update mojaloop-payment-manager-ui
sources:
  image:
    kind: dockerimage
    spec:
      image: pm4ml/mojaloop-payment-manager-ui
      versionfilter:
        kind: semver
        pattern: ^1.0.0
targets:
  yaml:
    kind: yaml
    spec:
      engine: yamlpath
      file: pm4ml-values-override.yaml
      key: $.frontend.image.tag
